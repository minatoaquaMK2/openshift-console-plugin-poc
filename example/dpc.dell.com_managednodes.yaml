---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: managednodes.dpc.dell.com
spec:
  group: dpc.dell.com
  names:
    kind: ManagedNode
    listKind: ManagedNodeList
    plural: managednodes
    singular: managednode
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedNode is the Schema for the managednodes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ManagedNode
            properties:
              membership:
                enum:
                - Present
                - Absent
                type: string
            type: object
          status:
            description: status defines the observed state of ManagedNode
            properties:
              conditions:
                description: Conditions represent the current state of the ManagedNode
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              inventory:
                description: Inventory contains hardware inventory data
                properties:
                  cooling:
                    description: CoolingSubsystem represents cooling components
                    properties:
                      fans:
                        items:
                          description: Fan represents a cooling fan
                          properties:
                            fqdd:
                              type: string
                            rpm:
                              type: integer
                            status:
                              type: string
                          required:
                          - rpm
                          type: object
                        type: array
                    type: object
                  cpu:
                    description: CPU represents CPU information
                    properties:
                      cores:
                        type: integer
                      model:
                        type: string
                      speed_mhz:
                        type: integer
                      threads:
                        type: integer
                    type: object
                  memory:
                    description: Memory represents memory information
                    properties:
                      speed_mhz:
                        type: integer
                      total_gb:
                        type: integer
                      type:
                        type: string
                    type: object
                  network:
                    description: NetworkSubsystem represents network components
                    properties:
                      adapters:
                        items:
                          description: NetworkAdapter represents network adapter
                          properties:
                            description:
                              type: string
                            fqdd:
                              type: string
                            mac_address:
                              type: string
                            speed_gbps:
                              type: integer
                            status:
                              type: string
                            type:
                              type: string
                          type: object
                        type: array
                    type: object
                  power:
                    description: PowerSubsystem represents power components
                    properties:
                      psus:
                        items:
                          description: PSU represents a power supply unit
                          properties:
                            fqdd:
                              type: string
                            input_watts:
                              type: string
                            status:
                              type: string
                          type: object
                        type: array
                    type: object
                  storage:
                    description: StorageSubsystem represents storage components
                    properties:
                      controllers:
                        items:
                          type: string
                        type: array
                      drives:
                        items:
                          description: Drive represents a storage drive
                          properties:
                            capacity_bytes:
                              format: int64
                              type: integer
                            fqdd:
                              type: string
                            slot_index:
                              type: integer
                            status:
                              type: string
                            type:
                              type: string
                          required:
                          - capacity_bytes
                          - slot_index
                          type: object
                        type: array
                    type: object
                  system_info:
                    description: SystemInfo contains basic system information
                    properties:
                      bios_version:
                        type: string
                      manufacturer:
                        type: string
                      model:
                        type: string
                      serial_number:
                        type: string
                    type: object
                type: object
              membership:
                properties:
                  conditions:
                    description: Conditions represent the current state of the membership sub-state machine
                    items:
                      description: Condition contains details for one aspect of the current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False, Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                  observedGeneration:
                    format: int64
                    type: integer
                  phase:
                    enum:
                    - Pending
                    - Running
                    - Succeeded
                    - Failed
                    type: string
                type: object
              lastCollectedAt:
                description: LastCollectedAt is when the inventory was last collected
                format: date-time
                type: string
              observedPlanGeneration:
                description: ObservedPlanGeneration tracks the InventoryRefreshPlan
                  generation
                format: int64
                type: integer
              observedRunId:
                description: ObservedRunId tracks the InventoryRefreshPlan run ID
                format: int64
                type: integer
              source:
                description: Source indicates where the inventory came from (k8s,
                  os, idrac)
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
